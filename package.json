{
  "name": "p5-sandsim",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --build src/main --watch",
    "start-run": "browser-sync start --server -w"
  },
  "devDependencies": {
    "@types/p5": "^1.3.1",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5",
    "ts-loader": "^9.2.6",
    "typescript": "^3.8.3",
    "webpack": "^5.57.1",
    "webpack-cli": "^4.8.0"
  }
}
